select * from tbl_board;

-- 게시판 테이블 
/*
CREATE TABLE public.tbl_board (
	bno int8 NOT NULL,
	title varchar(200) NOT NULL,
	"content" varchar(4000) NULL,
	writer varchar(50) NOT NULL,
	regdate timestamp NULL DEFAULT now(),
	viewcnt int8 NULL DEFAULT 0,
	CONSTRAINT bno_pkey PRIMARY KEY (bno)
);
*/

-- 댓글 테이블

/*
private int cno;
	private int bno;
	private String comment;
	private String writer;
	private String user_id;
	private Date regdate;*/

/*
create table comment(
	bno int8 not null,
	cno int8 not null,
	comment varchar(1000),
	writer varchar(50),
	user_id varchar(50),
	regdate timestamp NULL DEFAULT now(),
	CONSTRAINT cno_pkey PRIMARY KEY (cno),
	CONSTRAINT bno_fkey FOREIGN KEY (bno) REFERENCES tbl_board (bno)
);
*/

-- 페이징 테이블

/*private int nowPage, startPage, endPage, total, cntPerPage, lastPage, start, end;
	private int cntPage = 5;*/


create table paging(
	nowPage int8 not null,
	

)

